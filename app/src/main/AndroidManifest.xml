<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eat_it">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConfirmOrder"></activity>
        <activity android:name=".Cart" />
        <activity android:name=".AdmHomeActivity" />
        <activity android:name=".AdmCategoryActivity" />
        <activity android:name=".dr_signupui" />
        <activity android:name=".Userprofile" />
        <activity android:name=".FoodDetails" />
        <activity android:name=".AdminRegister" />
        <activity android:name=".Adminlogin" />
        <activity android:name=".Menu" />
        <activity android:name=".Login" />
        <activity android:name=".Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>